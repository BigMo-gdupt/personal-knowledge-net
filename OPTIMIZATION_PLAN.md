# 个人知识库项目优化方案

## 一、项目现状分析

### 1.1 项目结构概览

```
personal-knowlegel-net/
├── .obsidian/              # Obsidian配置
├── _staging_pdf/           # 待处理PDF（2篇论文）
├── raw/                    # LLM处理后的原始内容
│   ├── 2509.03505v2Limix/  # LimiX论文（217KB+文本+40张图）
│   └── Zeng_2025_Boiler/   # 锅炉论文（86KB+文本+20张图）
├── wiki/                   # 知识节点（9个md文件）
├── DEEPSEEK_SCHEMA.md      # LLM处理规范
└── analyze_*.py            # 分析脚本
```

### 1.2 当前问题诊断

| 问题类型 | 具体表现 | 影响 |
|---------|---------|------|
| **Token消耗过大** | 单篇论文处理需200万token | 成本高昂，无法持续 |
| **文件乱码** | 4个wiki文件出现编码问题 | 数据损坏，无法阅读 |
| **内容冗余** | raw/目录包含完整论文+所有图片 | 存储浪费，检索低效 |
| **处理流程重** | 全量论文喂给LLM分析 | 不必要的信息处理 |

### 1.3 Token消耗分析

以LimiX论文为例：
- 文本内容：约217KB ≈ 54,000 tokens（按4字符/token估算）
- 图片内容：40张图片，每张按1000 tokens描述 ≈ 40,000 tokens
- **总计约10万tokens/论文**，但用户提到需要200万tokens，说明：
  - 可能使用了高分辨率图片分析
  - 或者进行了多轮对话式分析
  - 或者包含了OCR后的密集文本

---

## 二、已修复问题

### 2.1 乱码文件修复 ✅

已修复以下4个文件：

| 文件名 | 问题 | 修复方式 |
|-------|------|---------|
| `LimiX模型.md` | 全文乱码 | 根据论文内容重新编写 |
| `多模态数据库.md` | 全文乱码 | 根据论文内容重新编写 |
| `水冷壁有效系数.md` | 全文乱码 | 根据论文内容重新编写 |
| `清洁度因子.md` | 全文乱码 | 根据论文内容重新编写 |
| `锅炉结渣与积灰预测.md` | 全文乱码 | 根据论文内容重新编写 |

**修复后状态**：所有wiki文件现在都有规范的YAML frontmatter、清晰的结构和正确的双向链接。

---

## 三、精简优化方案

### 方案A：手动精简模式（推荐）

**核心思想**：先人工阅读论文，提取关键知识点，再让LLM协助整理成标准格式。

#### 实施步骤

```
1. 阅读论文 → 2. 提取关键概念 → 3. 手动创建md → 4. LLM润色格式
```

#### 具体流程

**Step 1: 论文预处理（人工）**
- 快速浏览论文，标记关键章节
- 提取核心概念、公式、方法
- 记录需要深入理解的术语

**Step 2: 创建知识点草稿（人工）**
```markdown
# 草稿格式
- 概念名称：
- 定义：
- 公式：
- 关键引用：
- 相关概念：
```

**Step 3: LLM辅助格式化**
- 将草稿喂给DeepSeek
- 按DEEPSEEK_SCHEMA规范格式化
- 生成双向链接

**Token估算**：每知识点约500-1000 tokens，一篇论文约5-10个知识点 = **5,000-10,000 tokens**（比全量处理节省95%）

---

### 方案B：自动化精简模式

**核心思想**：使用轻量级工具预处理PDF，只提取关键内容给LLM。

#### 推荐工具链

```
PDF → Marker/PDFMiner → 提取章节结构 → 选择性喂给LLM
```

#### 实施步骤

**1. 安装轻量级PDF处理工具**
```bash
pip install marker-pdf pymupdf
```

**2. 创建预处理脚本**
```python
# extract_key_sections.py
import fitz  # PyMuPDF

def extract_sections(pdf_path, target_sections):
    """只提取目标章节内容"""
    doc = fitz.open(pdf_path)
    content = []
    for page in doc:
        text = page.get_text()
        # 根据目录或关键词筛选
        content.append(text)
    return "\n".join(content[:5000])  # 限制长度
```

**3. 分块处理策略**
- 摘要 + 引言：必处理
- 方法章节：核心处理
- 实验结果：选择性处理
- 相关工作：跳过
- 参考文献：跳过

**Token估算**：提取核心内容约5,000-10,000 tokens + LLM处理约5,000 tokens = **10,000-15,000 tokens/论文**

---

### 方案C：混合模式（平衡效率与深度）

**核心思想**：高频阅读用手动精简，批量处理用自动化工具。

#### 工作流设计

```
┌─────────────────────────────────────────────────────────┐
│  新论文到达                                              │
└────────────────────┬────────────────────────────────────┘
                     │
         ┌───────────┴───────────┐
         │                       │
    重要论文？               一般论文？
         │                       │
    ┌────┴────┐             ┌────┴────┐
    │   是    │             │   否    │
    └────┬────┘             └────┬────┘
         │                       │
  手动精简模式              自动精简模式
  (方案A)                   (方案B)
         │                       │
         └───────────┬───────────┘
                     │
              生成wiki节点
                     │
              建立双向链接
```

---

## 四、项目结构优化建议

### 4.1 推荐目录结构

```
personal-knowlegel-net/
├── .obsidian/                 # Obsidian配置
├── _inbox/                    # 待处理论文（PDF）
├── _templates/                # 笔记模板
│   ├── concept_template.md    # 概念节点模板
│   └── paper_template.md      # 论文总结模板
├── sources/                   # 精简后的源材料
│   ├── 2509.03505v2Limix_summary.md
│   └── Zeng_2025_Boiler_summary.md
├── wiki/                      # 知识节点（保持现状）
├── scripts/                   # 辅助脚本
│   ├── extract_sections.py    # PDF章节提取
│   ├── create_concept.py      # 创建概念节点
│   └── check_links.py         # 链接检查
└── README.md                  # 项目说明
```

### 4.2 新增模板文件

**concept_template.md**：
```markdown
---
tags: []
date: YYYY-MM-DD
source_refs: []
---

# 概念名称

一句话定义。

## 数学/逻辑推导

$$公式$$

## 前提条件与适用边界

### 前提条件
1. 

### 适用边界
1. 

## [关联拓扑]

- [[相关概念1]]
- [[相关概念2]]
```

---

## 五、实施建议

### 5.1 短期行动（本周）

- [x] 修复乱码文件
- [ ] 删除raw/目录中不必要的图片（可节省大量空间）
- [ ] 创建_templates/目录和模板文件
- [ ] 选择1篇新论文测试手动精简流程

### 5.2 中期优化（本月）

- [ ] 开发PDF章节提取脚本
- [ ] 建立论文阅读→知识点提取的标准工作流
- [ ] 完善wiki节点的双向链接网络

### 5.3 长期规划

- [ ] 考虑使用轻量级LLM（如本地部署的7B模型）进行初步处理
- [ ] 建立知识点复习/记忆系统
- [ ] 与Obsidian的图谱功能深度结合

---

## 六、Token成本对比

| 处理方式 | Token消耗/论文 | 成本估算 | 适用场景 |
|---------|---------------|---------|---------|
| 全量分析（原方案） | ~200万 | ¥10-20 | 不推荐 |
| 手动精简（方案A） | ~1万 | ¥0.05-0.1 | 重要论文 |
| 自动精简（方案B） | ~1.5万 | ¥0.08-0.15 | 批量处理 |
| 混合模式（方案C） | 视情况而定 | 平均¥0.1 | 推荐 |

---

## 七、总结

**核心建议**：

1. **放弃全量论文喂给LLM**的方式，改为**人工提取+LLM格式化**
2. **保留raw/目录但精简内容**，只保存文本摘要而非完整论文+图片
3. **建立标准化工作流**，提高知识提取效率
4. **优先使用方案A（手动精简）**，在熟悉流程后再考虑自动化

这样可以在保证知识质量的同时，将Token消耗降低95%以上，使项目可持续运行。
